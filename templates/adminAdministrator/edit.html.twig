{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <section class="card border-primary bg-primary text-white m-3">
        <h1 class="text-center text-uppercase p-3">Modification : {{ users.name|upper }}</h1>
    </section>

    <section class="card border-primary m-3">
        <div class="card-header bg-primary">
            <h3 class="text-center text-white">{{ users.name|upper }}</h3>
        </div>
        <div class="p-3">
            {{ include('adminAdministrator/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </section>

    <section class="m-3">
        <a href="{{ path('admin_administrator_index') }}">back to list</a>
            Back
        </a>
    </section>

{% endblock %}
