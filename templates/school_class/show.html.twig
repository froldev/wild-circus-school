{% extends 'base.html.twig' %}

{% block title %}SchoolClass{% endblock %}

{% block body %}
    <h1>SchoolClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ school_class.name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ school_class.duration }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ school_class.price }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ school_class.startDate }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ school_class.picture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ school_class.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('school_class_index') }}">back to list</a>

    <a href="{{ path('school_class_edit', {'id': school_class.id}) }}">edit</a>

    {{ include('school_class/_delete_form.html.twig') }}
{% endblock %}
