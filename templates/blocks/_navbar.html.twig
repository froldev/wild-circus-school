<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="{{ asset('build/images/logo.png') }}" width="auto" height="50" alt="Logo Wild Circus School">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('admin_home') }}">HOME</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_category_index') }}">CATEGORIES</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_class_index') }}">TRAINING</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_artist_index') }}">ARTISTS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_internship_index') }}">INTERNSHIPS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_partner_index') }}">PARTNERS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_administrator_index') }}">ADMINISTRATORS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_user_index') }}">USERS</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home') }}">HOME</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('school_class_index') }}">TRAINING</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('internship_index') }}">INTERNSHIP</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('inscription_index') }}">MY INSCRIPTION</a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="btn btn-outline-dark" href="{{ path('app_logout') }}">Log Out</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-outline-warning" href="{{ path('app_login') }}">Log In</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>