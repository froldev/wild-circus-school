{% extends 'base.html.twig' %}

{% block title %}Internship{% endblock %}

{% block body %}
    <h1>Internship</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internship.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ internship.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ internship.description }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ internship.startDate ? internship.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ internship.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('internship_index') }}">back to list</a>

    <a href="{{ path('internship_edit', {'id': internship.id}) }}">edit</a>

    {{ include('internship/_delete_form.html.twig') }}
{% endblock %}
