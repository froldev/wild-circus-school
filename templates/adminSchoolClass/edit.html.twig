{% extends 'base.html.twig' %}

{% block title %}Edit SchoolClass{% endblock %}

{% block body %}

    <section class="card border-secondary bg-primary text-white m-3">
        <h1 class="text-center text-uppercase p-3">
            Edit {{ class.name|upper }}
        </h1>
    </section>

    <section class="m-3">
        <div class="card col-xs-10 offset-xs-1 col-md-8 offset-md-2">
            <div class="card-body">
                {{ include('adminSchoolClass/_form.html.twig', {'button_label': 'Update'}) }}
                <a href="{{ path('admin_class_index') }}" class="btn btn-outline-dark col-2 m-1">Back</a>
            </div>
        </div>
    </section>

{% endblock %}
