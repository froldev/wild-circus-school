{% extends 'base.html.twig' %}

{% block title %}SchoolClass{% endblock %}

{% block body %}

    <section>
        <div class="card">
            <div class="row">
                <h1>{{ class.name }}</h1>
            </div>
            <div class="row">
                <div></div>
                <div></div>
            </div>
        </div>
    </section>

    <table class="table">
        <tbody>
            <tr>
                <th>Duration</th>
                <td>{{ class.duration }} month</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ class.price }} €</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ class.startDate|date("d/m/Y") }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>
                    <img src="{{ class.picture }}" alt="" height="250px"></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ class.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_class_index') }}">back to list</a>

    <a href="{{ path('admin_class_edit', {'id': class.id}) }}">edit</a>
{% endblock %}
